import { parseInfo } from "./parseInfo";
import { AnchorUSDTransferInfo, ApayTransferInfo, } from "../fixtures/TransferInfoResponse";
it("AnchorUSD: runs without error", function () {
    parseInfo(AnchorUSDTransferInfo);
});
it("Apay: runs without error", function () {
    parseInfo(ApayTransferInfo);
});
//# sourceMappingURL=parseInfo.test.js.map