import { __assign, __awaiter, __generator, __rest } from "tslib";
import isEqual from "lodash/isEqual";
import queryString from "query-string";
import { TransactionStatus } from "../constants/transfers";
import { parseInfo } from "./parseInfo";
function _normalizeTransaction(transaction) {
    if (transaction._id && transaction.id === undefined) {
        transaction.id = transaction._id;
    }
    if (transaction.amount &&
        transaction.amount_in === undefined &&
        transaction.amount_out === undefined) {
        transaction.amount_in = transaction.amount;
        transaction.amount_out = transaction.amount;
    }
    return transaction;
}
var TransferProvider = (function () {
    function TransferProvider(transferServer, account, lang, operation) {
        if (!transferServer) {
            throw new Error("Required parameter `transferServer` missing!");
        }
        if (!account) {
            throw new Error("Required parameter `account` missing!");
        }
        if (!operation) {
            throw new Error("Required parameter `operation` missing!");
        }
        this.transferServer = transferServer.replace(/\/$/, "");
        this.operation = operation;
        this.account = account;
        this.lang = lang;
        this._watchOneTransactionRegistry = {};
        this._watchAllTransactionsRegistry = {};
        this._transactionsRegistry = {};
        this._transactionsIgnoredRegistry = {};
        this._oneTransactionWatcher = {};
    }
    TransferProvider.prototype.fetchInfo = function () {
        return __awaiter(this, void 0, void 0, function () {
            var response, badResponse, _a, error, message, text, rawInfo, info;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0: return [4, fetch("".concat(this.transferServer, "/info?lang=").concat(this.lang))];
                    case 1:
                        response = _b.sent();
                        if (!!response.ok) return [3, 3];
                        return [4, response.text()];
                    case 2:
                        badResponse = _b.sent();
                        try {
                            _a = JSON.parse(badResponse), error = _a.error, message = _a.message;
                            throw new Error("Error fetching info from ".concat(this.transferServer, ": error ").concat(error ||
                                message));
                        }
                        catch (e) {
                            throw new Error("Error fetching info from ".concat(this.transferServer, ": error\n          code ").concat(response.status, ", response text: \"").concat(badResponse, "\""));
                        }
                        _b.label = 3;
                    case 3: return [4, response.text()];
                    case 4:
                        text = _b.sent();
                        try {
                            rawInfo = JSON.parse(text);
                            info = parseInfo(rawInfo);
                            this.info = info;
                            return [2, info];
                        }
                        catch (e) {
                            throw new Error("Error parsing the response of ".concat(this.transferServer, "/info as JSON: ").concat(text));
                        }
                        return [2];
                }
            });
        });
    };
    TransferProvider.prototype.getHeaders = function (headers) {
        if (headers === void 0) { headers = {}; }
        if (headers.Authorization && this.authToken) {
            throw new Error("You passed an `Authorization` header that will get wiped out by" +
                " SEP-10 auth");
        }
        return new Headers(this.authToken
            ? __assign(__assign({}, headers), { Authorization: "Bearer ".concat(this.authToken) }) : headers);
    };
    TransferProvider.prototype.setAuthToken = function (token) {
        this.authToken = token;
    };
    TransferProvider.prototype.fetchTransactions = function (params) {
        return __awaiter(this, void 0, void 0, function () {
            var isAuthRequired, kind, response, badResponse, _a, error, message, text, transactions;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        isAuthRequired = this.getAuthStatus("fetchTransactions", params.asset_code);
                        if (!params.show_all_transactions) {
                            kind =
                                params.kind ||
                                    (this.operation === "deposit" ? "deposit" : "withdrawal");
                        }
                        return [4, fetch("".concat(this.transferServer, "/transactions?").concat(queryString.stringify(__assign(__assign({}, params), { kind: kind }))), {
                                headers: isAuthRequired ? this.getHeaders() : undefined,
                            })];
                    case 1:
                        response = _b.sent();
                        if (!!response.ok) return [3, 3];
                        return [4, response.text()];
                    case 2:
                        badResponse = _b.sent();
                        try {
                            _a = JSON.parse(badResponse), error = _a.error, message = _a.message;
                            throw new Error("Error fetching transactions from ".concat(this.transferServer, ": error ").concat(error || message));
                        }
                        catch (e) {
                            throw new Error("Error fetching transactions from ".concat(this.transferServer, ": error\n          code ").concat(response.status, ", response text: \"").concat(badResponse, "\""));
                        }
                        _b.label = 3;
                    case 3: return [4, response.text()];
                    case 4:
                        text = _b.sent();
                        try {
                            transactions = JSON.parse(text).transactions;
                            return [2, transactions.map(_normalizeTransaction)];
                        }
                        catch (e) {
                            throw new Error("Invalid transaction response from ".concat(this.transferServer, ": ").concat(text));
                        }
                        return [2];
                }
            });
        });
    };
    TransferProvider.prototype.fetchTransaction = function (params, isWatching) {
        if (isWatching === void 0) { isWatching = false; }
        return __awaiter(this, void 0, void 0, function () {
            var asset_code, id, stellar_transaction_id, external_transaction_id, lang, isAuthRequired, qs, response, badResponse, _a, error, message, text, transaction;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        asset_code = params.asset_code, id = params.id, stellar_transaction_id = params.stellar_transaction_id, external_transaction_id = params.external_transaction_id, lang = params.lang;
                        if (id === undefined &&
                            stellar_transaction_id === undefined &&
                            external_transaction_id === undefined) {
                            throw new Error("fetchTransaction: One of `id`, `external_transaction_id`, " +
                                "or `stellar_transaction_id` must be provided! ");
                        }
                        isAuthRequired = this.getAuthStatus(isWatching ? "watchOneTransaction" : "fetchTransaction", asset_code);
                        qs = {};
                        if (id) {
                            qs = { id: id };
                        }
                        else if (stellar_transaction_id) {
                            qs = { stellar_transaction_id: stellar_transaction_id };
                        }
                        else if (external_transaction_id) {
                            qs = { external_transaction_id: external_transaction_id };
                        }
                        if (lang) {
                            qs = __assign({ lang: lang }, qs);
                        }
                        return [4, fetch("".concat(this.transferServer, "/transaction?").concat(queryString.stringify(qs)), {
                                headers: isAuthRequired ? this.getHeaders() : undefined,
                            })];
                    case 1:
                        response = _b.sent();
                        if (!!response.ok) return [3, 3];
                        return [4, response.text()];
                    case 2:
                        badResponse = _b.sent();
                        try {
                            _a = JSON.parse(badResponse), error = _a.error, message = _a.message;
                            throw new Error("Error fetching transaction ".concat(qs, " from ").concat(this.transferServer, ": error ").concat(error || message));
                        }
                        catch (e) {
                            throw new Error("Error fetching transaction ".concat(qs, " from ").concat(this.transferServer, ": error\n          code ").concat(response.status, ", response text: \"").concat(badResponse, "\""));
                        }
                        _b.label = 3;
                    case 3: return [4, response.text()];
                    case 4:
                        text = _b.sent();
                        try {
                            transaction = JSON.parse(text).transaction;
                            return [2, _normalizeTransaction(transaction)];
                        }
                        catch (e) {
                            throw new Error("Fetch transaction response wasn't valid JSON: ".concat(text));
                        }
                        return [2];
                }
            });
        });
    };
    TransferProvider.prototype.watchAllTransactions = function (params) {
        var _a;
        var _this = this;
        var asset_code = params.asset_code, onMessage = params.onMessage, onError = params.onError, _b = params.watchlist, watchlist = _b === void 0 ? [] : _b, _c = params.timeout, timeout = _c === void 0 ? 5000 : _c, _d = params.isRetry, isRetry = _d === void 0 ? false : _d, otherParams = __rest(params, ["asset_code", "onMessage", "onError", "watchlist", "timeout", "isRetry"]);
        var watchlistMap = watchlist.reduce(function (memo, id) {
            var _a;
            return (__assign(__assign({}, memo), (_a = {}, _a[id] = true, _a)));
        }, {});
        if (!isRetry) {
            this._watchAllTransactionsRegistry = __assign(__assign({}, this._watchAllTransactionsRegistry), (_a = {}, _a[asset_code] = true, _a));
        }
        this.fetchTransactions(__assign({ asset_code: asset_code }, (otherParams || {})))
            .then(function (transactions) {
            if (!_this._watchAllTransactionsRegistry[asset_code]) {
                return;
            }
            try {
                var newTransactions = transactions.filter(function (transaction) {
                    var isPending = transaction.status.indexOf("pending") === 0 ||
                        transaction.status === TransactionStatus.incomplete;
                    var registeredTransaction = _this._transactionsRegistry[asset_code][transaction.id];
                    if (!isRetry) {
                        if (watchlistMap[transaction.id]) {
                            return true;
                        }
                        if (!isPending) {
                            _this._transactionsIgnoredRegistry[asset_code][transaction.id] = transaction;
                        }
                        return isPending;
                    }
                    if (registeredTransaction) {
                        return !isEqual(registeredTransaction, transaction);
                    }
                    if ([
                        TransactionStatus.completed,
                        TransactionStatus.refunded,
                        TransactionStatus.error,
                    ].includes(transaction.status) &&
                        isRetry &&
                        !_this._transactionsIgnoredRegistry[asset_code][transaction.id]) {
                        return true;
                    }
                    if (isPending) {
                        return true;
                    }
                    return false;
                });
                newTransactions.forEach(function (transaction) {
                    _this._transactionsRegistry[asset_code][transaction.id] = transaction;
                    if (transaction.status === TransactionStatus.error) {
                        onError(transaction);
                    }
                    else {
                        onMessage(transaction);
                    }
                });
            }
            catch (e) {
                onError(e);
                return;
            }
            if (_this._allTransactionsWatcher) {
                clearTimeout(_this._allTransactionsWatcher);
            }
            _this._allTransactionsWatcher = setTimeout(function () {
                _this.watchAllTransactions(__assign({ asset_code: asset_code, onMessage: onMessage, onError: onError, timeout: timeout, isRetry: true }, (otherParams || {})));
            }, timeout);
        })
            .catch(function (e) {
            onError(e);
        });
        return {
            refresh: function () {
                if (!_this._watchAllTransactionsRegistry[asset_code]) {
                    return;
                }
                if (_this._allTransactionsWatcher) {
                    clearTimeout(_this._allTransactionsWatcher);
                }
                _this.watchAllTransactions(__assign({ asset_code: asset_code, onMessage: onMessage, onError: onError, timeout: timeout, isRetry: true }, (otherParams || {})));
            },
            stop: function () {
                if (_this._allTransactionsWatcher) {
                    _this._watchAllTransactionsRegistry[asset_code] = false;
                    _this._transactionsRegistry[asset_code] = {};
                    _this._transactionsIgnoredRegistry[asset_code] = {};
                    clearTimeout(_this._allTransactionsWatcher);
                }
            },
        };
    };
    TransferProvider.prototype.watchOneTransaction = function (params) {
        var _a, _b;
        var _this = this;
        var asset_code = params.asset_code, id = params.id, external_transaction_id = params.external_transaction_id, stellar_transaction_id = params.stellar_transaction_id, onMessage = params.onMessage, onSuccess = params.onSuccess, onError = params.onError, _c = params.timeout, timeout = _c === void 0 ? 5000 : _c, _d = params.isRetry, isRetry = _d === void 0 ? false : _d, otherParams = __rest(params, ["asset_code", "id", "external_transaction_id", "stellar_transaction_id", "onMessage", "onSuccess", "onError", "timeout", "isRetry"]);
        var txId = id || external_transaction_id || stellar_transaction_id;
        if (txId === undefined) {
            throw new Error("fetchTransaction: One of `id`, `external_transaction_id`, " +
                "or `stellar_transaction_id` must be provided! ");
        }
        if (!isRetry) {
            this._watchOneTransactionRegistry = __assign(__assign({}, this._watchOneTransactionRegistry), (_a = {}, _a[asset_code] = __assign(__assign({}, (this._watchOneTransactionRegistry[asset_code] || {})), (_b = {}, _b[txId] = true, _b)), _a));
        }
        this.fetchTransaction(__assign({ asset_code: asset_code, id: id }, (otherParams || {})), true)
            .then(function (transaction) {
            if (!(_this._watchOneTransactionRegistry[asset_code] &&
                _this._watchOneTransactionRegistry[asset_code][txId])) {
                return;
            }
            var registeredTransaction = _this._transactionsRegistry[asset_code][transaction.id];
            if (registeredTransaction &&
                isEqual(registeredTransaction, transaction)) {
                return;
            }
            _this._transactionsRegistry[asset_code][transaction.id] = transaction;
            if (transaction.status.indexOf("pending") === 0) {
                if (_this._oneTransactionWatcher[txId]) {
                    clearTimeout(_this._oneTransactionWatcher[txId]);
                }
                _this._oneTransactionWatcher[txId] = setTimeout(function () {
                    _this.watchOneTransaction(__assign({ asset_code: asset_code, id: id, external_transaction_id: external_transaction_id, stellar_transaction_id: stellar_transaction_id, onMessage: onMessage, onSuccess: onSuccess, onError: onError, timeout: timeout, isRetry: true }, (otherParams || {})));
                }, timeout);
                onMessage(transaction);
            }
            else if ([TransactionStatus.completed, TransactionStatus.refunded].includes(transaction.status)) {
                onSuccess(transaction);
            }
            else {
                onError(transaction);
            }
        })
            .catch(function (e) {
            onError(e);
        });
        return {
            refresh: function () {
                if (!(_this._watchOneTransactionRegistry[asset_code] &&
                    _this._watchOneTransactionRegistry[asset_code][txId])) {
                    return;
                }
                if (_this._oneTransactionWatcher[txId]) {
                    clearTimeout(_this._oneTransactionWatcher[txId]);
                }
                _this.watchOneTransaction(__assign({ asset_code: asset_code, id: id, external_transaction_id: external_transaction_id, stellar_transaction_id: stellar_transaction_id, onMessage: onMessage, onSuccess: onSuccess, onError: onError, timeout: timeout, isRetry: true }, (otherParams || {})));
            },
            stop: function () {
                if (_this._oneTransactionWatcher[txId]) {
                    _this._watchOneTransactionRegistry[asset_code][txId] = false;
                    clearTimeout(_this._oneTransactionWatcher[txId]);
                }
            },
        };
    };
    TransferProvider.prototype.fetchFinalFee = function (params) {
        return __awaiter(this, void 0, void 0, function () {
            var assetInfo, fee, _a, simpleFee, response, badResponse, _b, error, message, text, feeResponse;
            return __generator(this, function (_c) {
                switch (_c.label) {
                    case 0:
                        if (!this.info || !this.info[this.operation]) {
                            throw new Error("Run fetchSupportedAssets before running fetchFinalFee!");
                        }
                        assetInfo = this.info[this.operation][params.asset_code];
                        if (!assetInfo) {
                            throw new Error("Can't get fee for an unsupported asset, '".concat(params.asset_code));
                        }
                        fee = assetInfo.fee;
                        _a = fee.type;
                        switch (_a) {
                            case "none": return [3, 1];
                            case "simple": return [3, 2];
                            case "complex": return [3, 3];
                        }
                        return [3, 8];
                    case 1: return [2, 0];
                    case 2:
                        simpleFee = fee;
                        return [2, (((simpleFee.percent || 0) / 100) * Number(params.amount) +
                                (simpleFee.fixed || 0))];
                    case 3: return [4, fetch("".concat(this.transferServer, "/fee?").concat(queryString.stringify(__assign(__assign(__assign({}, params), fee), { operation: this.operation }))))];
                    case 4:
                        response = _c.sent();
                        if (!!response.ok) return [3, 6];
                        return [4, response.text()];
                    case 5:
                        badResponse = _c.sent();
                        try {
                            _b = JSON.parse(badResponse), error = _b.error, message = _b.message;
                            throw new Error("Error fetching fees from ".concat(this.transferServer, ": error ").concat(error ||
                                message));
                        }
                        catch (e) {
                            throw new Error("Error fetching fees from ".concat(this.transferServer, ": error\n              code ").concat(response.status, ", response text: \"").concat(badResponse, "\""));
                        }
                        _c.label = 6;
                    case 6: return [4, response.text()];
                    case 7:
                        text = _c.sent();
                        try {
                            feeResponse = JSON.parse(text).fee;
                            return [2, feeResponse];
                        }
                        catch (e) {
                            throw new Error("Fee endpoint returned invalid JSON: ".concat(text));
                        }
                        _c.label = 8;
                    case 8: throw new Error("Invalid fee type found! Got '".concat(fee.type, "' but expected one of 'none', 'simple', 'complex'"));
                }
            });
        });
    };
    TransferProvider.prototype.getAuthStatus = function (functionName, asset_code) {
        if (!asset_code) {
            throw new Error("Required parameter `asset_code` not provided!");
        }
        if (!this.info || !this.info[this.operation]) {
            throw new Error("Run fetchSupportedAssets before running ".concat(functionName, "!"));
        }
        var assetInfo = this.info[this.operation][asset_code];
        if (!assetInfo) {
            throw new Error("Asset ".concat(asset_code, " is not supported by ").concat(this.transferServer));
        }
        if (!this.authToken) {
            throw new Error("\n        Asset ".concat(asset_code, " requires authentication. Run KeyManager's\n        fetchAuthToken function, then run setAuthToken to set it.\n        "));
        }
        return true;
    };
    return TransferProvider;
}());
export { TransferProvider };
//# sourceMappingURL=TransferProvider.js.map