import { __assign, __awaiter, __generator, __spreadArray } from "tslib";
import * as StellarSdk from "@stellar/stellar-sdk";
import sinon from "sinon";
import { DepositProvider } from "./DepositProvider";
import { TransactionsResponse } from "../fixtures/TransactionsResponse";
import { SMXTransferInfo } from "../fixtures/TransferInfoResponse";
import { TransactionStatus } from "../constants/transfers";
var originalSetTimeout = global.setTimeout;
function sleep(time) {
    return new Promise(function (resolve) {
        originalSetTimeout(resolve, time);
    });
}
describe("fetchFinalFee", function () {
    test("AnchorUSD", function () { return __awaiter(void 0, void 0, void 0, function () {
        var info, provider, _a;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    info = {
                        USD: {
                            asset_code: "USD",
                            fee: { type: "simple", fixed: 5, percent: 1 },
                            min_amount: 15,
                            fields: [
                                {
                                    description: "your email address for transaction status updates",
                                    name: "email_address",
                                },
                                {
                                    description: "amount in USD that you plan to deposit",
                                    name: "amount",
                                },
                            ],
                        },
                    };
                    provider = new DepositProvider("test", StellarSdk.Keypair.random().publicKey());
                    provider.info = { deposit: info, withdraw: {} };
                    provider.authToken = "test";
                    _a = expect;
                    return [4, provider.fetchFinalFee({
                            asset_code: info.USD.asset_code,
                            amount: "15",
                        })];
                case 1:
                    _a.apply(void 0, [_b.sent()]).toEqual(5.15);
                    return [2];
            }
        });
    }); });
    test("EUR from Nucleo staging", function () { return __awaiter(void 0, void 0, void 0, function () {
        var info, provider, _a;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    info = {
                        EUR: {
                            asset_code: "EUR",
                            fee: { type: "simple", percent: 0.5 },
                            min_amount: 1,
                            max_amount: 1000000000,
                            fields: [
                                {
                                    description: "Type of deposit method for EUR",
                                    choices: ["SWIFT", "SEPA"],
                                    name: "type",
                                },
                            ],
                        },
                    };
                    provider = new DepositProvider("test", StellarSdk.Keypair.random().publicKey());
                    provider.info = { deposit: info, withdraw: {} };
                    provider.authToken = "test";
                    _a = expect;
                    return [4, provider.fetchFinalFee({
                            asset_code: info.EUR.asset_code,
                            amount: "10",
                        })];
                case 1:
                    _a.apply(void 0, [_b.sent()]).toEqual(0.05);
                    return [2];
            }
        });
    }); });
});
describe("watchOneTransaction", function () {
    var clock;
    var provider;
    var transferServer = "https://www.stellar.org/transfers";
    var pendingTransaction = function (eta, txStatus) {
        return {
            transaction: {
                kind: "deposit",
                id: "TEST",
                status: txStatus,
                status_eta: eta,
            },
        };
    };
    var successfulTransaction = function (eta, txStatus) {
        return {
            transaction: {
                kind: "deposit",
                id: "TEST",
                status: txStatus,
                status_eta: eta,
            },
        };
    };
    var failedTransaction = function (eta, txStatus) {
        return {
            transaction: {
                kind: "deposit",
                id: "TEST",
                status: txStatus,
                status_eta: eta,
            },
        };
    };
    beforeEach(function () { return __awaiter(void 0, void 0, void 0, function () {
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    clock = sinon.useFakeTimers(0);
                    fetch.resetMocks();
                    fetch.mockResponseOnce(JSON.stringify(SMXTransferInfo));
                    provider = new DepositProvider(transferServer, StellarSdk.Keypair.random().publicKey(), "uk-UA");
                    provider.authToken = "test";
                    return [4, provider.fetchSupportedAssets()];
                case 1:
                    _a.sent();
                    return [2];
            }
        });
    }); });
    afterEach(function () {
        clock.restore();
    });
    test("One completed success", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBe(0);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onSuccess.callCount).toBe(1);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify(successfulTransaction(0, TransactionStatus.completed)),
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.completed).transaction.id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("One refunded success", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBe(0);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onSuccess.callCount).toBe(1);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify(successfulTransaction(0, TransactionStatus.refunded)),
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.refunded).transaction.id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("One pending_user_transfer_start message", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBe(1);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onSuccess.callCount).toBe(0);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify(pendingTransaction(0, TransactionStatus.pending_user_transfer_start)),
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.pending_user_transfer_start).transaction.id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("One pending_user_transfer_complete message", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBe(1);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onSuccess.callCount).toBe(0);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify(pendingTransaction(0, TransactionStatus.pending_user_transfer_complete)),
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.pending_user_transfer_complete).transaction.id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("One pending_anchor message", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBe(1);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onSuccess.callCount).toBe(0);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify(pendingTransaction(0, TransactionStatus.pending_anchor)),
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.pending_anchor).transaction
                            .id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("One error", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBe(0);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onSuccess.callCount).toBe(0);
                    });
                    onError = sinon.spy(function () {
                        expect(onError.callCount).toBe(1);
                    });
                    fetch.mockResponses([
                        JSON.stringify(failedTransaction(0, TransactionStatus.error)),
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.error).transaction.id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(1);
                    return [2];
            }
        });
    }); });
    test("One no_market", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBe(0);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onSuccess.callCount).toBe(0);
                    });
                    onError = sinon.spy(function () {
                        expect(onError.callCount).toBe(1);
                    });
                    fetch.mockResponses([
                        JSON.stringify(failedTransaction(0, TransactionStatus.no_market)),
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.no_market).transaction.id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(1);
                    return [2];
            }
        });
    }); });
    test("Several pending transactions", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThanOrEqual(7);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onSuccess.callCount).toBe(0);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify(pendingTransaction(0, TransactionStatus.pending_anchor)),
                        { status: 200 },
                    ], [
                        JSON.stringify(pendingTransaction(1, TransactionStatus.pending_external)),
                        { status: 200 },
                    ], [
                        JSON.stringify(pendingTransaction(2, TransactionStatus.pending_stellar)),
                        { status: 200 },
                    ], [
                        JSON.stringify(pendingTransaction(3, TransactionStatus.pending_trust)),
                        { status: 200 },
                    ], [
                        JSON.stringify(pendingTransaction(4, TransactionStatus.pending_user)),
                        { status: 200 },
                    ], [
                        JSON.stringify(pendingTransaction(5, TransactionStatus.pending_user_transfer_complete)),
                        { status: 200 },
                    ], [
                        JSON.stringify(pendingTransaction(6, TransactionStatus.pending_user_transfer_start)),
                        { status: 200 },
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.pending_anchor).transaction
                            .id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 1:
                    _a.sent();
                    clock.next();
                    return [4, sleep(10)];
                case 2:
                    _a.sent();
                    clock.next();
                    return [4, sleep(10)];
                case 3:
                    _a.sent();
                    clock.next();
                    return [4, sleep(10)];
                case 4:
                    _a.sent();
                    clock.next();
                    return [4, sleep(10)];
                case 5:
                    _a.sent();
                    clock.next();
                    return [4, sleep(10)];
                case 6:
                    _a.sent();
                    clock.next();
                    return [4, sleep(10)];
                case 7:
                    _a.sent();
                    expect(onMessage.callCount).toBe(7);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("One pending, one completed, no more after that", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(2);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(2);
                    });
                    onError = sinon.spy(function (transaction) {
                        expect(transaction).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify(pendingTransaction(0, TransactionStatus.pending_user_transfer_complete)),
                        { status: 200 },
                    ], [
                        JSON.stringify(successfulTransaction(0, TransactionStatus.completed)),
                        { status: 200 },
                    ], [
                        JSON.stringify(successfulTransaction(0, TransactionStatus.completed)),
                        { status: 200 },
                    ], [
                        JSON.stringify(successfulTransaction(0, TransactionStatus.completed)),
                        { status: 200 },
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.completed).transaction.id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 2:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 3:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("One pending, one refunded, no more after that", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(2);
                    });
                    onSuccess = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(2);
                    });
                    onError = sinon.spy(function (transaction) {
                        expect(transaction).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify(pendingTransaction(0, TransactionStatus.pending_user_transfer_complete)),
                        { status: 200 },
                    ], [
                        JSON.stringify(successfulTransaction(0, TransactionStatus.refunded)),
                        { status: 200 },
                    ], [
                        JSON.stringify(successfulTransaction(0, TransactionStatus.refunded)),
                        { status: 200 },
                    ], [
                        JSON.stringify(successfulTransaction(0, TransactionStatus.refunded)),
                        { status: 200 },
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: successfulTransaction(0, TransactionStatus.refunded).transaction.id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 2:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 3:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("One pending, one error, no more after that", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(2);
                    });
                    onSuccess = sinon.spy(function (transaction) {
                        expect(transaction).toBeUndefined();
                    });
                    onError = sinon.spy(function () {
                        expect(onError.callCount).toBeLessThan(2);
                    });
                    fetch.mockResponses([
                        JSON.stringify(pendingTransaction(0, TransactionStatus.pending_anchor)),
                        { status: 200 },
                    ], [
                        JSON.stringify(failedTransaction(0, TransactionStatus.error)),
                        { status: 200 },
                    ], [
                        JSON.stringify(failedTransaction(0, TransactionStatus.error)),
                        { status: 200 },
                    ], [
                        JSON.stringify(failedTransaction(0, TransactionStatus.error)),
                        { status: 200 },
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: pendingTransaction(0, TransactionStatus.pending_anchor).transaction
                            .id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 2:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(1);
                    clock.next();
                    return [4, sleep(10)];
                case 3:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(1);
                    return [2];
            }
        });
    }); });
    test("One pending, one no_market, no more after that", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(2);
                    });
                    onSuccess = sinon.spy(function (transaction) {
                        expect(transaction).toBeUndefined();
                    });
                    onError = sinon.spy(function () {
                        expect(onError.callCount).toBeLessThan(2);
                    });
                    fetch.mockResponses([
                        JSON.stringify(pendingTransaction(0, TransactionStatus.pending_anchor)),
                        { status: 200 },
                    ], [
                        JSON.stringify(failedTransaction(0, TransactionStatus.no_market)),
                        { status: 200 },
                    ], [
                        JSON.stringify(failedTransaction(0, TransactionStatus.no_market)),
                        { status: 200 },
                    ], [
                        JSON.stringify(failedTransaction(0, TransactionStatus.no_market)),
                        { status: 200 },
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: pendingTransaction(0, TransactionStatus.pending_anchor).transaction
                            .id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 2:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(1);
                    clock.next();
                    return [4, sleep(10)];
                case 3:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(1);
                    return [2];
            }
        });
    }); });
    test("Two pending, one error, no more after that", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onSuccess, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(3);
                    });
                    onSuccess = sinon.spy(function (transaction) {
                        expect(transaction).toBeUndefined();
                    });
                    onError = sinon.spy(function () {
                        expect(onError.callCount).toBeLessThan(2);
                    });
                    fetch.mockResponses([
                        JSON.stringify(pendingTransaction(0, TransactionStatus.pending_user_transfer_start)),
                        { status: 200 },
                    ], [
                        JSON.stringify(pendingTransaction(1, TransactionStatus.pending_user_transfer_complete)),
                        { status: 200 },
                    ], [
                        JSON.stringify(failedTransaction(2, TransactionStatus.error)),
                        { status: 200 },
                    ], [
                        JSON.stringify(failedTransaction(3, TransactionStatus.error)),
                        { status: 200 },
                    ], [
                        JSON.stringify(failedTransaction(4, TransactionStatus.error)),
                        { status: 200 },
                    ]);
                    provider.watchOneTransaction({
                        asset_code: "SMX",
                        id: pendingTransaction(0, TransactionStatus.pending_user_transfer_start)
                            .transaction.id,
                        onMessage: onMessage,
                        onSuccess: onSuccess,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 2:
                    _a.sent();
                    expect(onMessage.callCount).toBe(2);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    clock.next();
                    return [4, sleep(10)];
                case 3:
                    _a.sent();
                    expect(onMessage.callCount).toBe(2);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(1);
                    clock.next();
                    return [4, sleep(10)];
                case 4:
                    _a.sent();
                    expect(onMessage.callCount).toBe(2);
                    expect(onSuccess.callCount).toBe(0);
                    expect(onError.callCount).toBe(1);
                    return [2];
            }
        });
    }); });
});
describe("watchAllTransactions", function () {
    var clock;
    var provider;
    var transferServer = "https://www.stellar.org/transfers";
    beforeEach(function () { return __awaiter(void 0, void 0, void 0, function () {
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    clock = sinon.useFakeTimers(0);
                    fetch.resetMocks();
                    fetch.mockResponseOnce(JSON.stringify(SMXTransferInfo));
                    provider = new DepositProvider(transferServer, StellarSdk.Keypair.random().publicKey(), "uk-UA");
                    provider.authToken = "test";
                    return [4, provider.fetchSupportedAssets()];
                case 1:
                    _a.sent();
                    return [2];
            }
        });
    }); });
    afterEach(function () {
        clock.restore();
    });
    test("Return only pending", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(4);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([JSON.stringify(TransactionsResponse)]);
                    provider.watchAllTransactions({
                        asset_code: "SMX",
                        onMessage: onMessage,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(3);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("Return two changed thing", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onError, _a, firstResponseA, restA, updatedTransactions, firstResponseB, secondResponseB, restB;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(6);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([JSON.stringify(TransactionsResponse)]);
                    provider.watchAllTransactions({
                        asset_code: "SMX",
                        onMessage: onMessage,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    return [4, sleep(1)];
                case 1:
                    _b.sent();
                    expect(onMessage.callCount).toBe(3);
                    expect(onError.callCount).toBe(0);
                    _a = TransactionsResponse.transactions, firstResponseA = _a[0], restA = _a.slice(1);
                    updatedTransactions = __spreadArray([
                        __assign(__assign({}, firstResponseA), { status: TransactionStatus.completed })
                    ], restA, true);
                    fetch.mockResponses([
                        JSON.stringify(__assign(__assign({}, TransactionsResponse), { transactions: updatedTransactions })),
                    ]);
                    clock.next();
                    return [4, sleep(10)];
                case 2:
                    _b.sent();
                    expect(onMessage.callCount).toBe(4);
                    expect(onError.callCount).toBe(0);
                    return [4, sleep(1)];
                case 3:
                    _b.sent();
                    expect(onMessage.callCount).toBe(4);
                    expect(onError.callCount).toBe(0);
                    firstResponseB = updatedTransactions[0], secondResponseB = updatedTransactions[1], restB = updatedTransactions.slice(2);
                    updatedTransactions = __spreadArray([
                        firstResponseB,
                        __assign(__assign({}, secondResponseB), { status: TransactionStatus.refunded })
                    ], restB, true);
                    fetch.mockResponses([
                        JSON.stringify(__assign(__assign({}, TransactionsResponse), { transactions: updatedTransactions })),
                    ]);
                    clock.next();
                    return [4, sleep(10)];
                case 4:
                    _b.sent();
                    expect(onMessage.callCount).toBe(5);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("Immediate completed get messages", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(2);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify({
                            status: true,
                            transactions: [],
                        }),
                    ]);
                    provider.watchAllTransactions({
                        asset_code: "SMX",
                        onMessage: onMessage,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    fetch.mockResponses([
                        JSON.stringify({
                            status: true,
                            transactions: [
                                {
                                    id: 74,
                                    order_id: "ord_2mPmw2EBF5iCN2nWv",
                                    status: "completed",
                                    receiving_account_number: "646180111812345678",
                                    receiving_account_bank: "STP",
                                    memo: null,
                                    memo_type: null,
                                    email_address: null,
                                    type: "SPEI",
                                    asset_code: "SMX",
                                    expires_at: "1970-01-19T06:08:23.883Z",
                                    created_at: "2019-09-26T19:58:03.856Z",
                                    phone: "+14155099007",
                                    amount: "25.00",
                                    account: "GARBRF35ZWKO4MIEY7RPHHPQ74P45TWNMIQ5VKOTXF7KYVFJBGWW2IMQ",
                                    transaction_id: "DI8AADD303D3686399",
                                    external_transaction_id: "DI8AADD303D3686399",
                                    kind: "deposit",
                                },
                            ],
                        }),
                    ]);
                    clock.next();
                    return [4, sleep(10)];
                case 2:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    fetch.mockResponses([
                        JSON.stringify({
                            status: true,
                            transactions: [
                                {
                                    id: 74,
                                    order_id: "ord_2mPmw2EBF5iCN2nWv",
                                    status: "completed",
                                    receiving_account_number: "646180111812345678",
                                    receiving_account_bank: "STP",
                                    memo: null,
                                    memo_type: null,
                                    email_address: null,
                                    type: "SPEI",
                                    asset_code: "SMX",
                                    expires_at: "1970-01-19T06:08:23.883Z",
                                    created_at: "2019-09-26T19:58:03.856Z",
                                    phone: "+14155099007",
                                    amount: "25.00",
                                    account: "GARBRF35ZWKO4MIEY7RPHHPQ74P45TWNMIQ5VKOTXF7KYVFJBGWW2IMQ",
                                    transaction_id: "DI8AADD303D3686399",
                                    external_transaction_id: "DI8AADD303D3686399",
                                    kind: "deposit",
                                },
                            ],
                        }),
                    ]);
                    clock.next();
                    return [4, sleep(10)];
                case 3:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
    test("Immediate refunded get messages", function () { return __awaiter(void 0, void 0, void 0, function () {
        var onMessage, onError;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    onMessage = sinon.spy(function () {
                        expect(onMessage.callCount).toBeLessThan(2);
                    });
                    onError = sinon.spy(function (e) {
                        expect(e).toBeUndefined();
                    });
                    fetch.mockResponses([
                        JSON.stringify({
                            status: true,
                            transactions: [],
                        }),
                    ]);
                    provider.watchAllTransactions({
                        asset_code: "SMX",
                        onMessage: onMessage,
                        onError: onError,
                        timeout: 10,
                        lang: "uk-UA",
                    });
                    expect(onMessage.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    return [4, sleep(1)];
                case 1:
                    _a.sent();
                    expect(onMessage.callCount).toBe(0);
                    expect(onError.callCount).toBe(0);
                    fetch.mockResponses([
                        JSON.stringify({
                            status: true,
                            transactions: [
                                {
                                    id: 77,
                                    order_id: "ord_2mRPdvpUjPN2rfuS7",
                                    status: "refunded",
                                    receiving_account_number: "646180111812345678",
                                    receiving_account_bank: "STP",
                                    memo: null,
                                    memo_type: null,
                                    email_address: null,
                                    type: "SPEI",
                                    asset_code: "SMX",
                                    expires_at: "1970-01-19T09:23:30.864Z",
                                    created_at: "2019-10-01T20:34:24.753Z",
                                    phone: "+14155099007",
                                    amount: "25.00",
                                    account: "GARBRF35ZWKO4MIEY7RPHHPQ74P45TWNMIQ5VKOTXF7KYVFJBGWW2IMQ",
                                    transaction_id: "DI938432727A0B2456",
                                    external_transaction_id: "DI938432727A0B2456",
                                    kind: "deposit",
                                },
                            ],
                        }),
                    ]);
                    clock.next();
                    return [4, sleep(10)];
                case 2:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    fetch.mockResponses([
                        JSON.stringify({
                            status: true,
                            transactions: [
                                {
                                    id: 77,
                                    order_id: "ord_2mRPdvpUjPN2rfuS7",
                                    status: "refunded",
                                    receiving_account_number: "646180111812345678",
                                    receiving_account_bank: "STP",
                                    memo: null,
                                    memo_type: null,
                                    email_address: null,
                                    type: "SPEI",
                                    asset_code: "SMX",
                                    expires_at: "1970-01-19T09:23:30.864Z",
                                    created_at: "2019-10-01T20:34:24.753Z",
                                    phone: "+14155099007",
                                    amount: "25.00",
                                    account: "GARBRF35ZWKO4MIEY7RPHHPQ74P45TWNMIQ5VKOTXF7KYVFJBGWW2IMQ",
                                    transaction_id: "DI938432727A0B2456",
                                    external_transaction_id: "DI938432727A0B2456",
                                    kind: "deposit",
                                },
                            ],
                        }),
                    ]);
                    clock.next();
                    return [4, sleep(10)];
                case 3:
                    _a.sent();
                    expect(onMessage.callCount).toBe(1);
                    expect(onError.callCount).toBe(0);
                    return [2];
            }
        });
    }); });
});
describe("validateFields", function () {
    var asset_code = "TEST";
    function getProvider(fields) {
        var _a;
        var provider = new DepositProvider("https://test.com", StellarSdk.Keypair.random().publicKey(), "en-US");
        provider.info = {
            deposit: (_a = {},
                _a[asset_code] = {
                    asset_code: asset_code,
                    fee: { type: "simple" },
                    fields: fields,
                    min_amount: 0,
                },
                _a),
            withdraw: {},
        };
        provider.authToken = "test";
        return provider;
    }
    test("returns true for empty fields", function () {
        expect(getProvider([]).validateFields(asset_code, {})).toEqual(true);
    });
    test("returns true for correct response", function () {
        var fields = [
            {
                description: "your email address for transaction status updates",
                optional: true,
                name: "email_address",
            },
            {
                description: "amount in cents that you plan to deposit",
                name: "amount",
            },
            {
                description: "type of deposit to make",
                choices: ["SPEI", "cash"],
                name: "type",
            },
        ];
        expect(getProvider(fields).validateFields(asset_code, {
            email: "asdf@asdf.com",
            amount: "10",
            type: "SPEI",
        })).toEqual(true);
    });
    test("returns false for invalid type", function () {
        var fields = [
            {
                description: "your email address for transaction status updates",
                optional: true,
                name: "email_address",
            },
            {
                description: "amount in cents that you plan to deposit",
                name: "amount",
            },
            {
                description: "type of deposit to make",
                choices: ["SPEI", "cash"],
                name: "type",
            },
        ];
        expect(getProvider(fields).validateFields(asset_code, {
            email: "asdf@asdf.com",
            amount: "10",
            type: "asdlkfjasldkfjaskdlfjask",
        })).toEqual(false);
    });
    test("returns false for invalid email", function () {
        var fields = [
            {
                description: "your email address for transaction status updates",
                name: "email_address",
            },
            {
                description: "amount in cents that you plan to deposit",
                name: "amount",
            },
            {
                description: "type of deposit to make",
                choices: ["SPEI", "cash"],
                name: "type",
            },
        ];
        expect(getProvider(fields).validateFields(asset_code, {
            email: "asdf",
            amount: "10",
            type: "cash",
        })).toEqual(false);
    });
    test("returns false for invalid amount", function () {
        var fields = [
            {
                description: "your email address for transaction status updates",
                optional: true,
                name: "email_address",
            },
            {
                description: "amount in cents that you plan to deposit",
                name: "amount",
            },
            {
                description: "type of deposit to make",
                choices: ["SPEI", "cash"],
                name: "type",
            },
        ];
        expect(getProvider(fields).validateFields(asset_code, {
            email: "asdf@asdf.com",
            amount: "asdf",
            type: "cash",
        })).toEqual(false);
    });
    test("real-world case", function () {
        var fields = [
            {
                description: "Enter the amount in ARS that you plan to deposit",
                name: "amount",
            },
            { description: "Enter your email address", name: "email_address" },
        ];
        var payload = {
            amount: "255",
            asset_code: "ARS",
            email_address: "cassio@stellar.org",
        };
        expect(getProvider(fields).validateFields(asset_code, payload)).toEqual(true);
    });
});
//# sourceMappingURL=DepositProvider.test.js.map