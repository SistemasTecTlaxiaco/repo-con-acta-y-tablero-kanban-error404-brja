import { getKeyMetadata } from "./getKeyMetadata";
describe("getKeyMetadata", function () {
    test("ledger key", function () {
        var encryptedKey = {
            id: "PURIFIER",
            encryptedBlob: "BLOB",
            encrypterName: "Test",
            salt: "SLFKJSDLKFJLSKDJFLKSJD",
        };
        expect(getKeyMetadata(encryptedKey)).toEqual({
            id: "PURIFIER",
        });
    });
});
//# sourceMappingURL=getKeyMetadata.test.js.map