import { __awaiter, __generator } from "tslib";
import { KeyType } from "../constants/keys";
import { testEncrypter } from "../PluginTesting";
import { ScryptEncrypter } from "./ScryptEncrypter";
test("encrypts and decrypts a key", function () { return __awaiter(void 0, void 0, void 0, function () {
    var key, password, encryptedKey, decryptedKey;
    return __generator(this, function (_a) {
        switch (_a.label) {
            case 0:
                key = {
                    type: KeyType.plaintextKey,
                    publicKey: "AVACYN",
                    privateKey: "ARCHANGEL",
                    id: "PURIFIER",
                    path: "PATH",
                    extra: "EXTRA",
                };
                password = "This is a really cool password and is good";
                return [4, ScryptEncrypter.encryptKey({
                        key: key,
                        password: password,
                    })];
            case 1:
                encryptedKey = _a.sent();
                expect(encryptedKey).toBeTruthy();
                expect(encryptedKey.encryptedBlob).toBeTruthy();
                expect(encryptedKey.encryptedBlob).not.toEqual(key.privateKey);
                return [4, ScryptEncrypter.decryptKey({
                        encryptedKey: encryptedKey,
                        password: password,
                    })];
            case 2:
                decryptedKey = _a.sent();
                expect(decryptedKey.privateKey).not.toEqual(encryptedKey.encryptedBlob);
                expect(decryptedKey).toEqual(key);
                return [2];
        }
    });
}); });
it("passes PluginTesting", function () { return __awaiter(void 0, void 0, void 0, function () {
    var _a;
    return __generator(this, function (_b) {
        switch (_b.label) {
            case 0:
                _a = expect;
                return [4, testEncrypter(ScryptEncrypter)];
            case 1:
                _a.apply(void 0, [_b.sent()]).toEqual(true);
                return [2];
        }
    });
}); });
//# sourceMappingURL=ScryptEncrypter.test.js.map