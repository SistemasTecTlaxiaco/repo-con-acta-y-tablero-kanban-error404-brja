import { EncryptedKey, KeyMetadata, KeyStore } from "../types";
import { BrowserStorageConfigParams } from "./BrowserStorageFacade";
export declare class BrowserStorageKeyStore implements KeyStore {
    name: string;
    private keyStore;
    constructor();
    configure(params: BrowserStorageConfigParams): Promise<void>;
    storeKeys(keys: EncryptedKey[]): Promise<KeyMetadata[]>;
    updateKeys(keys: EncryptedKey[]): Promise<KeyMetadata[]>;
    loadKey(id: string): Promise<any>;
    removeKey(id: string): Promise<KeyMetadata>;
    loadAllKeys(): Promise<EncryptedKey[]>;
}
