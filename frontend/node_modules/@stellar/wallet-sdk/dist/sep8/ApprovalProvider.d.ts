import { FeeBumpTransaction, Transaction } from "@stellar/stellar-sdk";
import { ApprovalResponse, PostActionUrlRequest, PostActionUrlResponse } from "../types/sep8";
export declare class ApprovalProvider {
    approvalServer: string;
    constructor(approvalServer: string);
    approve(transaction: Transaction | FeeBumpTransaction, contentType?: "application/json" | "application/x-www-form-urlencoded"): Promise<ApprovalResponse>;
    postActionUrl(params: PostActionUrlRequest): Promise<PostActionUrlResponse>;
}
