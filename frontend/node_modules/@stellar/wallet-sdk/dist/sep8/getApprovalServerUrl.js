import { __awaiter, __generator } from "tslib";
import { StellarToml } from "@stellar/stellar-sdk";
export function getApprovalServerUrl(param, opts) {
    if (opts === void 0) { opts = {}; }
    return __awaiter(this, void 0, void 0, function () {
        var tomlObject, _i, _a, ast;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    if (!param.home_domain) {
                        throw new Error("Issuer's home domain is missing");
                    }
                    return [4, StellarToml.Resolver.resolve(param.home_domain, opts)];
                case 1:
                    tomlObject = _b.sent();
                    if (!tomlObject.CURRENCIES) {
                        throw new Error("stellar.toml at ".concat(param.home_domain, " does not contain CURRENCIES field"));
                    }
                    for (_i = 0, _a = tomlObject.CURRENCIES; _i < _a.length; _i++) {
                        ast = _a[_i];
                        if (ast.code === param.asset_code && ast.issuer === param.asset_issuer) {
                            if (!ast.approval_server) {
                                throw new Error("stellar.toml at ".concat(param.home_domain, " does not contain approval_server information for this asset"));
                            }
                            return [2, ast.approval_server];
                        }
                    }
                    throw new Error("CURRENCY ".concat(param.asset_code, ":").concat(param.asset_issuer, " not found on stellar.toml at ").concat(param.home_domain));
            }
        });
    });
}
//# sourceMappingURL=getApprovalServerUrl.js.map