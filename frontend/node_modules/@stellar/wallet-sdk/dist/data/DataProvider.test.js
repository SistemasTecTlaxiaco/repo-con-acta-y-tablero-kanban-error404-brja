import { Networks } from "@stellar/stellar-sdk";
import { generatePlaintextKey } from "../fixtures/keys";
import { DataProvider } from "./DataProvider";
describe("Account validation", function () {
    test("works with null values", function () {
        try {
            var provider = new DataProvider({
                accountOrKey: null,
                serverUrl: "https://horizon.stellar.org",
                networkPassphrase: Networks.PUBLIC,
            });
            expect(provider).not.toBeInstanceOf(DataProvider);
        }
        catch (e) {
            expect(e).toBeTruthy();
            expect(e.toString()).toBe("Error: No account key provided.");
        }
    });
    test("works with real public keys", function () {
        try {
            var provider = new DataProvider({
                accountOrKey: "GDZBHQFIHLVDF6GCRV5DT2STB6ZXAJR3JFGZNXNPLB35TH5GNMUVIAQP",
                serverUrl: "https://horizon.stellar.org",
                networkPassphrase: Networks.PUBLIC,
            });
            expect(provider).toBeInstanceOf(DataProvider);
        }
        catch (e) {
            expect(e).toBeUndefined();
        }
    });
    test("works with real typed Keys", function () {
        try {
            var provider = new DataProvider({
                accountOrKey: generatePlaintextKey(),
                serverUrl: "https://horizon.stellar.org",
                networkPassphrase: Networks.PUBLIC,
            });
            expect(provider).toBeInstanceOf(DataProvider);
        }
        catch (e) {
            expect(e).toBeUndefined();
        }
    });
    test("Throw with bad key", function () {
        var provider;
        try {
            provider = new DataProvider({
                accountOrKey: "I am not a stupid key you dumbdumb",
                serverUrl: "https://horizon.stellar.org",
                networkPassphrase: Networks.PUBLIC,
            });
        }
        catch (e) {
            expect(e).toBeTruthy();
        }
        expect(provider).not.toBeInstanceOf(DataProvider);
    });
});
//# sourceMappingURL=DataProvider.test.js.map