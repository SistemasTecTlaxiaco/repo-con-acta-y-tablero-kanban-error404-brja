import { Asset } from "@stellar/stellar-sdk";
export function getTokenIdentifier(token) {
    if (token.type === "native") {
        return "native";
    }
    return "".concat(token.code, ":").concat(token.issuer.key);
}
export function getBalanceIdentifier(balance) {
    if ("asset_issuer" in balance && !balance.asset_issuer) {
        return "native";
    }
    switch (balance.asset_type) {
        case "credit_alphanum4":
        case "credit_alphanum12":
            return "".concat(balance.asset_code, ":").concat(balance.asset_issuer);
        case "liquidity_pool_shares":
            return "".concat(balance.liquidity_pool_id, ":lp");
        default:
            return "native";
    }
}
export function getStellarSdkAsset(token) {
    if (token.type === "native") {
        return Asset.native();
    }
    return new Asset(token.code, token.issuer.key);
}
//# sourceMappingURL=index.js.map