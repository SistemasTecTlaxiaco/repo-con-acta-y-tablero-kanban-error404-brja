[package]
name = "logintech_contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "0.40.0" # Ajusta a la versión que tengas instalada
serde = { version = "1.0", features = ["derive"] }

[profile.release]
opt-level = "z"
